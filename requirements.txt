# Install from this repository root with:
#   python -m pip install -r requirements.txt
# Runtime dependencies are declared only in pyproject.toml. The dev extra adds
# pytest so every handoff recipient can run the supplied tests.
-e .[dev]
